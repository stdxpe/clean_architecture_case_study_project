part of '../utilities/utilities_library_imports.dart';

/// [APP-WIDE GLOBAL VARIABLE]

enum LogLevel { info, debug, error, warning, fatal }

enum HttpMethod { get, post, put, patch, delete }

enum AppLocale { en, tr }

enum MediaSource { camera, gallery }

enum NotificationMode { none, snackbar, dialog, toast }

enum ProvideMode { singleton, lazySingleton, factory, subInstance }
